<Conformance xmlns="http://hl7.org/fhir">
  <!-- from Resource: extension, modifierExtension, language, text, and contained -->
  <publisher value="MITRE"/><!-- 1..1 Publishing Organization § -->
  <description value="Test implementation of the FHIR markup for our database"/><!-- ?? 0..1 Human description of the conformance statement § -->
  <experimental value="true"/><!-- 0..1 If for testing purposes, not real usage § -->
  <date value="2015-07-10T09:08:29.499+10:00"/><!-- 1..1 Publication Date § -->
  <fhirVersion value="0.0.82"/><!-- 1..1 FHIR Version § -->
  <acceptUnknown value="false"/><!-- 1..1 True if application accepts unknown elements § -->
  <format value="xml"/>
  <format value="json"/><!-- 1..* formats supported (xml | json | mime type) -->
  <rest>  <!-- ?? 0..* If the endpoint is a RESTful one -->
    <mode value="server"/>
    <documentation value="Only implements practitioners"/>
    <resource>  <!-- 1..* Resource served on the REST interface -->
      <type value="Practitioner"/><!-- 1..1 A resource type that is supported -->
      <!-- <profile>0..1 Resource(Profile) What structural features are supported

      </profile> -->
      <operation>  <!-- 1..* What operations are supported? -->
        <code value="read"/>
        <documentation value="can only retrieve via ids"/><!-- 0..1 Anything special about operation behavior -->
      </operation>
      <readHistory value="false"/><!-- 0..1 Whether vRead can return past versions -->
      <updateCreate value="false"/><!-- 0..1 If allows/uses update to a new location -->
    </resource>
  </rest>
</Conformance>